---
date: 2026-06-03
---
# Step 1.4: narrow settings/ and dialogs/ to their final scopes

**Context:** Fourth sub-step of `Phase 1: file moves` in `plans/frontend-component-organization.md`. After this step, `dialogs/` contains only the global `PasswordPromptDialog.svelte`, and `settings/` is the settings domain (host + 3 tabs + `CaptionOptionsFields`).

**Decision:** Three moves with `git mv`, three import sites updated via `sed`. Did NOT move `TemplateManager.svelte` — it's marked LEGACY in the existing `frontend-architecture.md` memory and is superseded by the `/templates` route. Moving it now would create a one-file "template legacy" sub-folder for no current value. Leaving it in `settings/` until a real deprecation/cleanup pass.

**Result:**
- `dialogs/SettingsDialog.svelte` → `settings/SettingsDialog.svelte`
- `dialogs/GeneralSettings.svelte` → `settings/GeneralSettings.svelte`
- `dialogs/SecuritySettings.svelte` → `settings/SecuritySettings.svelte`

**Import updates:**
- `lib/components/settings/SettingsDialog.svelte` → 2 imports updated (now self-references the new sibling locations)
- `routes/+layout.svelte` → 1 import updated

**Final folder state:**
- `lib/components/dialogs/` — `PasswordPromptDialog.svelte` only (global utility)
- `lib/components/settings/` — `SettingsDialog.svelte`, `GeneralSettings.svelte`, `SecuritySettings.svelte`, `CaptionOptionsFields.svelte`, `TemplateManager.svelte` (legacy, see above)

**Verification:** `svelte-check` → 0 errors / 0 warnings. `vite build` → success.
