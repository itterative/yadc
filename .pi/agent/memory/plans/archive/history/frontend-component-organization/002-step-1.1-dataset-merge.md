---
date: 2026-06-03
---
# Step 1.1: merge dataset/ + datasets/ into dataset/ subfolders

**Context:** First sub-step of `Phase 1: file moves` in `plans/archive/frontend-component-organization.md`. Pure rename + folder reorganization, no behavior change.

**Decision:** Used `git mv` for all 14 file moves so git tracks them as renames. Updated 7 import sites across routes and lib via `sed` (path renames are mechanical and well-known).

**Result:**
- `dataset/DatasetBrowser.svelte` + `dataset/DatasetImage.svelte` → `dataset/browser/`
- `dataset/ImageDetail/*` → `dataset/detail/` (folder rename)
- `datasets/DatasetConfig{Form,Advanced}.svelte` + `datasets/ConfigHistory.svelte` + `datasets/datasetConfig/{patch.ts,state.svelte.ts}` → `dataset/config/` (the `datasetConfig/` sub-helper folder was also collapsed since helpers are now siblings of their consumer)
- `datasets/{DatasetUploadPanel,UploadDatasetTab,CreateDatasetTab}.svelte` → `dataset/upload/`
- `datasets/DatasetManageTab.svelte` → `dataset/manage/`
- Empty `datasets/` directory removed

**Relative import changes inside `dataset/config/`:**
- `./datasetConfig/state.svelte` → `./state.svelte` (3 files: `DatasetConfig.svelte`, `DatasetConfigForm.svelte`, `DatasetConfigAdvanced.svelte`)
- `./datasetConfig/patch` → `./patch` (1 file: `DatasetConfig.svelte`)

**Verification:** `svelte-check` → 0 errors / 0 warnings. `vite build` → success. `eslint .` → clean.
