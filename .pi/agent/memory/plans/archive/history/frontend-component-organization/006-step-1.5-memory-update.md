---
date: 2026-06-03
---
# Step 1.5: memory updates (frontend-architecture + todo)

**Context:** Fifth and final sub-step of `Phase 1: file moves` in `plans/archive/frontend-component-organization.md`. No code changes — pure documentation refresh after the structural moves.

**Decision:**

1. **`.pi/agent/memory/frontend-architecture.md`**:
   - Replaced the **Directory Structure** tree (top of file) with the up-to-date layout: `dataset/` sub-folders, no `datasets/`, no stale `routes/datasets/[name]/DatasetConfig.svelte` reference, no `routes/datasets/[name]/CaptionSettings.svelte` reference.
   - Replaced the **Component Organization** section (was a bulleted list) with the locked-in two-rule framework (scope rule + feature-folder rule) plus a description of each `lib/components/<domain>/` sub-folder.
   - Updated the `stores/` section to list all 14 store files (was missing several: `captionActions.ts`, `captionSettings.ts`, `confirm.ts`, `passwordPrompt.ts`, `sessionPassword.ts`, `storageStore.ts`, `toasts.ts`, `topbar.svelte.ts`).
   - Added `animations.css` to the `styles/` block (was missing).
   - Added `Card`/`ActionBar`/`ActionBarItem` to the `ui/` block (the misc-fixes rebase brought these in but the memory didn't reflect them yet).

2. **`.pi/agent/memory/todo.md`**:
   - Marked `## Frontend remaining cleanup` as **DONE (Phase 1)** — the structural items it covered are done. Kept the 2 specific deferred items (SidePanel class prop, Tooltip z-index) as in-line follow-ups since they're separate from the org work.
   - Re-purposed `## Sidebar / topbar refactor` from "needs cleanup" to "visual polish" — the structural move (left sidebar + topbar) is done. The 2 specific bullets stay open as visual TODOs. Per user feedback: this is a visual task, not a structural one, and should not be marked done.

**Result:** Both memory files now reflect the post-Phase-1 reality and the locked-in conventions.

**Verification:** `svelte-check` → 0 errors / 0 warnings. `eslint .` → clean. No code touched.
