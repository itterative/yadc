---
date: 2026-06-03
---
# Step 1.2: extract env/ and export/ domains

**Context:** Second sub-step of `Phase 1: file moves` in `plans/archive/frontend-component-organization.md`. Pure rename + folder reorganization, no behavior change.

**Decision:** Three file moves with `git mv`, three import sites updated via `sed`. `dialogs/` and `settings/` folders are intentionally NOT removed in this step — step 1.4 will move the remaining files out of them.

**Result:**
- `dialogs/EnvironmentSettings.svelte` → `env/EnvironmentSettings.svelte`
- `settings/EnvSelector.svelte` → `env/EnvSelector.svelte`
- `dialogs/ExportDialog.svelte` → `export/ExportDialog.svelte`

**Import updates:**
- `lib/components/dialogs/SettingsDialog.svelte` → `$lib/components/env/EnvironmentSettings.svelte`
- `routes/datasets/[name]/CaptionSettings.svelte` → `$lib/components/env/EnvSelector.svelte`
- `routes/+layout.svelte` → `$lib/components/export/ExportDialog.svelte`

**Verification:** `svelte-check` → 0 errors / 0 warnings. `vite build` → success.
